version: '2.2'

services:
  vcc:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}vcc
    depends_on:
      - databaseds
      - rsyslog-csplmc
      - vcccapabilities
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc/$$i ||\
             tango_admin --add-server Vcc/cbf Vcc mid_csp_cbf/vcc/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/Vcc/Vcc.py cbf"
             
  vcccapabilities:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}vcccapabilities
    depends_on:
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_band12/$$i ||\
             tango_admin --add-server VccBand1And2/cbf VccBand1And2 mid_csp_cbf/vcc_band12/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/VccBand1And2/VccBand1And2.py cbf &\
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_band3/$$i ||\
             tango_admin --add-server VccBand3/cbf VccBand3 mid_csp_cbf/vcc_band3/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/VccBand3/VccBand3.py cbf &\
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_band4/$$i ||\
             tango_admin --add-server VccBand4/cbf VccBand4 mid_csp_cbf/vcc_band4/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/VccBand4/VccBand4.py cbf &\
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_band5/$$i ||\
             tango_admin --add-server VccBand5/cbf VccBand5 mid_csp_cbf/vcc_band5/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/VccBand5/VccBand5.py cbf &\
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_tdc1/$$i ||\
             tango_admin --add-server VccTransientDataCapture/cbf VccTransientDataCapture mid_csp_cbf/vcc_tdc1/$$i; done &&\
             for i in $$(seq -w 1 197); do \
             tango_admin --check-device mid_csp_cbf/vcc_tdc2/$$i ||\
             tango_admin --add-server VccTransientDataCapture/cbf VccTransientDataCapture mid_csp_cbf/vcc_tdc2/$$i; done &&\
             /venv/bin/python /app/csplmc/Vcc/VccTransientDataCapture/VccTransientDataCapture.py cbf"

  fsp:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}fsp
    depends_on:
      - databaseds
      - rsyslog-csplmc
      - fspcapabilities
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 27); do \
             tango_admin --check-device mid_csp_cbf/fsp/$$i ||\
             tango_admin --add-server Fsp/cbf Fsp mid_csp_cbf/fsp/$$i; done &&\
             /venv/bin/python /app/csplmc/Fsp/Fsp/Fsp.py cbf"
             
  fspcapabilities:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}fspcapabilities
    depends_on:
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 27); do \
             tango_admin --check-device mid_csp_cbf/fsp_corr/$$i ||\
             tango_admin --add-server FspCorr/cbf FspCorr mid_csp_cbf/fsp_corr/$$i; done &&\
             /venv/bin/python /app/csplmc/Fsp/FspCorr/FspCorr.py cbf &\
             for i in $$(seq -w 1 27); do \
             tango_admin --check-device mid_csp_cbf/fsp_pss/$$i ||\
             tango_admin --add-server FspPss/cbf FspPss mid_csp_cbf/fsp_pss/$$i; done &&\
             /venv/bin/python /app/csplmc/Fsp/FspPss/FspPss.py cbf &\
             for i in $$(seq -w 1 27); do \
             tango_admin --check-device mid_csp_cbf/fsp_pst/$$i ||\
             tango_admin --add-server FspPst/cbf FspPst mid_csp_cbf/fsp_pst/$$i; done &&\
             /venv/bin/python /app/csplmc/Fsp/FspPst/FspPst.py cbf &\
             for i in $$(seq -w 1 27); do \
             tango_admin --check-device mid_csp_cbf/fsp_vlbi/$$i ||\
             tango_admin --add-server FspVlbi/cbf FspVlbi mid_csp_cbf/fsp_vlbi/$$i; done &&\
             /venv/bin/python /app/csplmc/Fsp/FspVlbi/FspVlbi.py cbf"
             
  fspsubarray:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}fspsubarray
    depends_on:
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 27); do \
             for j in $$(seq -w 1 16); do \
             tango_admin --check-device mid_csp_cbf/fspSubarray/$$i\_$$j ||\
             tango_admin --add-server FspSubarray/cbf FspSubarray mid_csp_cbf/fspSubarray/$$i\_$$j &&\
             echo mid_csp_cbf/fspSubarray/$$i\_$$j; done; done &&\
             /venv/bin/python /app/csplmc/Fsp/FspSubarray/FspSubarray.py cbf"

  cbfsubarray:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}cbfsubarray
    depends_on:
      - vcc
      - fsp
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             for i in $$(seq -w 1 16); do \
             tango_admin --check-device mid_csp_cbf/cbfSubarray/$$i ||\
             tango_admin --add-server CbfSubarray/cbf CbfSubarray mid_csp_cbf/cbfSubarray/$$i; done &&\
             /venv/bin/python /app/csplmc/CbfSubarray/CbfSubarray/CbfSubarray.py cbf"

  cbfmaster:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}cbfmaster
    depends_on:
      - cbfsubarray
      - vcc
      - fsp
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             tango_admin --check-device mid_csp_cbf/master/main ||\
             tango_admin --add-server CbfMaster/cbf CbfMaster mid_csp_cbf/master/main &&\
             /venv/bin/python /app/csplmc/CbfMaster/CbfMaster/CbfMaster.py cbf"

  tmtelstatetest:
    image: ${DOCKER_REGISTRY_HOST}/${DOCKER_REGISTRY_USER}/csplmc:latest
    network_mode: ${NETWORK_MODE}
    container_name: ${CONTAINER_NAME_PREFIX}tmtelstatetest
    depends_on:
      - databaseds
      - rsyslog-csplmc
    environment:
      - TANGO_HOST=${TANGO_HOST}
    command: >
      sh -c "wait-for-it.sh ${TANGO_HOST} --timeout=30 --strict --
             tango_admin --check-device ska1_mid/tm/telmodel ||\
             tango_admin --add-server TmTelstateTest/tm TmTelstateTest ska1_mid/tm/telmodel &&\
             /venv/bin/python /app/csplmc/TmTelstateTest/TmTelstateTest.py tm"
             
  rsyslog-csplmc:
    image: jumanjiman/rsyslog
    container_name: ${CONTAINER_NAME_PREFIX}rsyslog-csplmc
    network_mode: ${NETWORK_MODE}
